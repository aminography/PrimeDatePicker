<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aminography.primedatepicker.sample">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.DatePickerActivity"
            android:label="@string/title_date_picker_activity" />

        <activity
            android:name=".view.CalendarViewActivity"
            android:label="@string/title_calendar_activity"
            android:theme="@style/AppTheme.NoActionBar.SmokyStatus" />

        <activity
            android:name=".view.MonthViewActivity"
            android:label="@string/title_month_activity"
            android:theme="@style/AppTheme.NoActionBar.SmokyStatus" />

    </application>

</manifest>
